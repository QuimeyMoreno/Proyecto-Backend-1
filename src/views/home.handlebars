{{#each products.docs}}
  <div>
    <h2>{{this.title}}</h2>
    <p>{{this.description}}</p>
    <p>Precio: ${{this.price}}</p>
    <button onclick="addToCart('{{../cartId}}', '{{this._id}}')">Agregar al Carrito</button>
  </div>
{{/each}}

<script src="/assets/js/home.js"></script>
